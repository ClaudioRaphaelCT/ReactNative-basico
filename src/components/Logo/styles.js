import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  logoSpace: {},
  logoTitle: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: 'bold',
    fontStyle: 'italic',
    color: 'blue',
  },
});

export default styles;
